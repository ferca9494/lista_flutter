import 'package:flutter/animation.dart';

/// A placeholder class that represents an entity or model.
class Category {
  Category(this.id, this.nombre, this.color);

  final int id;
  final String nombre;
  final Color color;
}
