/// A placeholder class that represents an entity or model.
import 'category.dart';

class SampleItem {
  SampleItem(this.id, this.nombre, this.cantidad, this.precio, this.categoria);

  final int id;
  final String nombre;
  final int cantidad;
  final double precio;
  final Category categoria;
}
