/// A placeholder class that represents an entity or model.
class SampleItem {
  const SampleItem(this.id, this.nombre, this.cantidad, this.precio);

  final int id;
  final String nombre;
  final int cantidad;
  final double precio;
}
